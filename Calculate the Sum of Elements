def sum_of_elements(lst): # defining the function
    total = 0
    for element in lst:
        total += element
    return total

# Prints the sum of elements
print(sum_of_elements([1, 2, 3, 4, 5]))  # Output: 15 # calling the function
