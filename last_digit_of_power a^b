def last_digit(a, b):
    # Calculate a^b and return the last digit
    return (a ** b) % 10

# Example usage
a = int(input("Enter the base (a): "))
b = int(input("Enter the exponent (b): "))

print(f"The last digit of {a}^{b} is: {last_digit(a, b)}")

