def print_mirror_pyramid(size):
    # Upper part of the pyramid
    for i in range(1, size + 1):
        print(" " * (size - i) + "* " * i)

    # Lower part of the pyramid
    for i in range(size, 0, -1):
        print(" " * (size - i) + "* " * i)

size = 7
print_mirror_pyramid(size)
